procs:
  trunk: "trunk serve"
  tailwind: "npx tailwindcss -i ./input.css -o ./style/generated.css --watch"
  fmt: 
    shell: "cargo fmt"
    autostart: false
  tests:
    shell: "cargo test"
    autostart: false